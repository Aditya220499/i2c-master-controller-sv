`timescale 1ns/1ps

module start_gen_tb;

    // ============================================================
    // CONTROL
    // ============================================================

    logic start_enable;

    // ============================================================
    // BUS WIRES
    // ============================================================

    wire sda;
    wire scl;

    // ============================================================
    // BUS OBSERVATION
    // ============================================================

    logic sda_in;
    logic scl_in;

    // ============================================================
    // DRIVE CONTROL
    // ============================================================

    logic sda_drive_low;
    logic scl_drive_low;

    // ============================================================
    // PULL-UP RESISTORS
    // ============================================================

    pullup(sda);
    pullup(scl);

    // ============================================================
    // DUT
    // ============================================================

    start_gen dut (

        .start_enable (start_enable),

        .scl_in       (scl_in),
        .sda_in       (sda_in),

        .sda_drive_low(sda_drive_low),
        .scl_drive_low(scl_drive_low)

    );

    // ============================================================
    // OPEN-DRAIN BUS MODEL
    // ============================================================

    assign sda = (sda_drive_low) ? 1'b0 : 1'bz;
    assign scl = (scl_drive_low) ? 1'b0 : 1'bz;

    // ============================================================
    // OBSERVE BUS
    // ============================================================

    assign sda_in = sda;
    assign scl_in = scl;

    // ============================================================
    // TEST SEQUENCE
    // ============================================================

    initial begin

        $display("\n======================================");
        $display("START CONDITION GENERATION TEST");
        $display("======================================\n");

        // ========================================================
        // IDLE BUS
        // ========================================================

        start_enable = 0;

        #50;

        // ========================================================
        // GENERATE START
        // ========================================================

        start_enable = 1;

        #50;

        // ========================================================
        // RELEASE CONTROL
        // ========================================================

        start_enable = 0;

        #50;

        $display("\n======================================");
        $display("SIMULATION COMPLETE");
        $display("======================================\n");

        $finish;

    end

endmodule